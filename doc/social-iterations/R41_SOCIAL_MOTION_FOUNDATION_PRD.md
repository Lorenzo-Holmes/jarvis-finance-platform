# R41 需求：社交页面 Motion Foundation

问题：社区与个人中心大量交互仍使用浏览器默认瞬时切换，与 Workspace System 已有动效语言脱节。

目标：为两页建立统一的 social motion token，复用 workspace 的 press/state/surface/layout 时长，并补全 prefers-reduced-motion。

验收：后续交互动效统一从 token 取值；系统减少动态设置下动画与平滑滚动被关闭。
