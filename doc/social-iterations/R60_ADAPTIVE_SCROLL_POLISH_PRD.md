# R60 需求：响应式滚动与粘性导航收尾

问题：长列表与窄屏筛选已经可用，但滚动体验仍偏浏览器默认：Tab 滚出视口、目录滚动会传播到页面、横向筛选停在任意半截位置。

目标：社区 Tab sticky；内部目录/消息线程 contain overscroll；桌面使用细滚动条；移动端横向 Tab/筛选使用 scroll-snap；个人中心指标侧栏在桌面保持可见。

验收：不制造双滚动页面；窄屏横向控件可自然吸附；980px 以下关闭 sticky aside；reduced-motion 不受影响。
