# R60 构建设计

Community tablist 改 sticky top=0 并增加 translucent material。目录/线程使用 scrollbar-gutter、overscroll-behavior 和细滚动条。700px 横向 Tab 使用 scroll-snap。Profile aside 桌面 sticky，移动端恢复 static；filters 使用 snap + 隐藏滚动条。
