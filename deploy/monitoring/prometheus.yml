global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/jarvis-alerts.yml

scrape_configs:
  - job_name: jarvis-java
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["127.0.0.1:8201"]
        labels:
          service: jarvis-java
